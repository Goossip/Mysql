#列属性：定义或这创建一个列的时候，可以给列额外增加“附加特性”
create table 表名 (列名 列类型 [列属性]);
说明：
1.一个列可以有多个列属性
2.多个列属性空格隔开就行
列属性如下：
#null , not null
设定为空或非空，表明该列数据是否可为空值(null)
#default
用于设定列默认值（不给值或给空值null并not null，就会自动使用该值）
使用形式：default 默认值
#primary key
#主键的另一种设定方式
create table pri_test (
id int auto_increment,
name varchar(10),
primary key(id)
);
用于设定主键
主键就是一个表中数据的”关键值“，通过该关键值就可以找到该特定的数据行
一个表中的主键值不能重复（相等），比如文章表中的id，比如用户表中的用户名
主键字段必须有值，不能为空
一个表只能有一个主键，但一个主键可以是1个字段或者2个以上的字段联合构成
#auto_increment
用于设定一个整数字段的值是自增长的，通常用于一个表中数据行的编号，比如文章编号
默认情况下自增长值是从1开始的
一个表只能设定一个字段位自增长特性
#unique key用于设定唯一键的特性
唯一键表示一个表中的某个字段的值是唯一的，不可重复的
唯一键类似primary key,但其值可以为null
一个表可以有多个唯一键
#comment
用于设定字段的说明性内容，类似注释，但又不是注释，属于有效的代码
使用形式：comment ’文字内容‘
#查询一张表所有字段信息
show full columns from iib_2019;